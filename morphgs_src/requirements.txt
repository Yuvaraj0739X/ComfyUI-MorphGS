# Pure-Python requirements of the vendored MorphGS source, for standalone use.
# Inside ComfyUI-MorphGS these are installed from the package's top-level requirements.txt.
# torch / torchvision / pytorch3d / gsplat are installed separately (see README).
numpy
scipy
pytorch-msssim
scikit-image
Pillow
opencv-python
trimesh
imageio
imageio-ffmpeg
matplotlib
tqdm
pyyaml
fpsample
jaxtyping
einops
omegaconf
# Dropped as unused by the code paths this copy actually runs: open3d, scikit-learn and
# pynocchio (only utils/rigging_pinocchio.py, which nothing imports), scikit-sparse (RigModel
# falls back to scipy when it is absent), pymeshlab, pyrender, plyfile, typeguard, pykeops.
